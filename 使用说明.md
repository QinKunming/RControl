# RControl 远程控制 使用说明

直连式远程控制软件（无中继服务器），被控端 Windows 7 / Windows Server 2019 / Win10 / Win11，
主控端 Windows 7 及以上。支持远程画面、键鼠控制、文件传输、剪贴板同步、锁屏画面下的登录操作。

---

## 一、被控端安装（Windows 7 / Server 2019+）

1. 把 `rcontrol_server.exe` 放到固定目录（如 `C:\rcontrol\`），**不要放桌面或临时目录**。
   **Win7 / Server 2008 R2 用 `被控端-Win7` 目录里的 exe；Server 2019 / Win10 / Win11 用
   `被控端-Win2019以上` 目录里的 exe**（两者功能相同，Win7 版是独立编译的兼容版本）。
2. **右键"以管理员身份运行"**，打开设置窗口：
   - 首次运行会自动生成访问口令（可自行修改）
   - 监听端口默认 `3333`，可任意修改（要求 > 1024）
3. 两种运行方式：

   | 方式 | 操作 | 效果 |
   |---|---|---|
   | **服务模式（推荐）** | 点"安装服务"→"启动服务" | 开机自启；**锁屏/未登录时也能被控制和登录**；用户注销后自动重连 |
   | 用户模式 | 点"重启工作进程" | 仅当前用户登录期间可用，锁屏后无法登录 |

   服务模式安装时会自动添加防火墙入站规则（TCP 监听端口）。
   修改端口后需重新点"安装服务"（或手动改防火墙规则）。

4. 卸载：设置窗口点"停止服务"→"卸载服务"，删除 exe 和 `C:\ProgramData\rcontrol\` 即可。

配置文件位于 `C:\ProgramData\rcontrol\config.toml`（端口/口令/帧率/画质），
在 GUI 里改并"保存设置"后工作进程会自动重启生效。

## 二、主控端连接（Windows 7+）

1. 运行 `rcontrol_client.exe`（Win7 用 `主控端-Win7` 目录里的版本，其余系统用 `主控端-Win10以上`）
2. 在"被控端清单"里选中一台**双击**（或填好参数点"连接"）
3. 窗口内即远程桌面，鼠标键盘直接操作

### 被控端清单管理

主控端窗口上半部分是**被控端清单**，可保存多台被控端：

- **双击**清单里的一台直接发起连接
- **新增到清单**：在下方"连接参数"里填 名称 / 地址 / 口令 / 是否仅查看，点"新增到清单"
- **更新选中**：先单击清单里的一台（参数填入下方表单），修改后点"更新选中"
- **删除选中**：选中后删除（有确认弹窗）
- 清单自动保存在主控端软件所在目录的 **`hosts.txt`**（`名称<Tab>地址<Tab>口令<Tab>只读` 格式，
  口令明文保存，请自行保管好该文件；也可用记事本手工编辑）
- 地址格式 `IP:端口`，不填端口默认 `3333`
- 首次使用无清单文件时，自动带入旧版的"上次连接"地址

### 画面显示（1:1 原始分辨率）

远程画面按**被控端原始分辨率 1:1 像素显示**，不做比例缩放，两边的点击位置严格一致：

- 主控窗口放不下整个画面时，右侧/底部出现滚动条：**滚轮**平移（Shift+滚轮横向）、
  **拖拽滚动条滑块**、点击滚动条轨道翻页
- 主控窗口比画面大时，画面居中显示
- **初始窗口大小**：主控端显示器放得下画面 → 窗口正好是画面原始尺寸（居中）；
  放不下 → 窗口自动**最大化**占满主控屏，用滚动条平移
- 标题栏显示当前画面分辨率（如 `[3840x2160 1:1]`），连接后立刻可见——
  没有这个字样说明运行的是旧版 exe
- 被控端 `config.toml` 的 `max_width` 默认 `0`（发送原始分辨率）；
  网速吃紧时可在被控端 GUI 把"最大宽度"设为如 `1920` 压缩带宽
  （`0` = 原始分辨率；画面相应缩小，仍然 1:1 无比例失真）

### 快捷键

| 按键 | 功能 |
|---|---|
| `Esc` | 断开连接 |
| `F9` | 打开文件传输窗口 |
| `Ctrl+Shift+Del` | 向被控端发送"安全注意序列"（见下） |
| `Ctrl+Alt+Insert` | 同上（备用，个别程序吞 Del 键时用） |

### 远程屏幕锁定时如何登录

前提：被控端以**服务模式**运行（客户端标题栏会显示，无"[被控端未装服务]"字样即正常）。

1. 被控端锁屏后，远程画面会切换显示锁屏界面（可能稍暗）
2. 按 `Ctrl+Shift+Del` —— 相当于在被控端物理键盘上按 Ctrl+Alt+Del，弹出登录框
3. **直接输入被控端的 Windows 登录密码**，回车即可登录（键盘输入直达锁屏界面）
4. 登录后画面自动切回桌面

被控端开机停在登录界面（无人登录）时，同样操作可直接远程登录进系统。

## 三、文件传输

连接后按 `F9` 打开文件管理器（双栏资源管理器样式，带文件图标、大小、修改时间、类型四列信息）：

- 左边是**远程**（被控端）目录，右边是**本地**（主控端）目录
- 浏览目录的几种方式：
  - **双击**文件夹（或盘符）进入，双击 `..` 返回上级；选中后按**回车**同样进入
  - 顶部路径框可以直接输入完整路径（如 `D:\work`），按**回车**或点“转到”跳转
  - `↑ 上级`、`刷新` 按钮；初始为驱动器列表，双击盘符进入
- 每栏都有 `新建文件夹`、`删除`、`重命名` 按钮（删除有确认弹窗，文件夹整棵删除）
- 传文件（支持按住 Ctrl/Shift 多选）：
  - `→ 下载到本地`：远程栏选中的文件 → 本地栏当前目录
  - `← 上传到远程`：本地栏选中的文件 → 远程栏当前目录
  - 覆盖同名文件会先弹窗确认
- 底部状态栏实时显示进度和速度；大文件自动分块流式传输，不占额外内存（实测 1MB/2.5MB 往返 SHA256 一致）
- 注意：暂不支持直接传输文件夹，请先压缩成压缩包
- 传输期间按钮自动禁用防并发，`Esc` 关闭文件管理器（不影响远程控制连接）

## 四、剪贴板

双向自动同步**文本**（两边复制的文字，另一边可直接粘贴），约 1 秒内生效，最大 4MB。

## 五、安全机制

- 端到端 AES-256-GCM 加密（密钥由口令 + 双方随机数派生，每次连接不同）
- 双向认证：口令错误无法连接，且能防服务端伪造
- 同一密码连续错 5 次，该 IP 被封 60 秒
- 同一时间只接受一个主控连接
- 纯直连：数据不经过任何第三方服务器

## 六、自检命令

主控端 exe 支持自动化自测（对被控端做 11 项检查：认证/画面/键鼠/文件/剪贴板）：

```
rcontrol_client.exe --selftest IP:端口 口令
```

全部通过输出 `11 通过 / 0 失败`，退出码 0。

命令行直连（跳过对话框）：

```
rcontrol_client.exe --view IP:端口 口令
```

## 七、常见问题

**连不上？**
- 被控端防火墙：服务模式会自动加规则；用户模式需手动放行端口
- 端口/口令是否与被控端 GUI 显示的一致
- 被控端状态栏显示"未运行"时点"重启工作进程"（或启动服务）

**画面卡/糊？** 被控端 GUI 里调画质（低/中/高）和最大帧率；网速有保障时用高画质。
4K 原始分辨率每帧约 0.5~1MB，动态画面对带宽要求较高（百兆局域网可流畅）；
网速不足时在 `config.toml` 设 `max_width = 1920` 降压。

**键鼠无效？** 确认连接时没勾"仅查看"；被控端如果弹出 UAC 提权框，画面会暂时无法操作（Windows 安全限制，服务模式下按 Ctrl+Shift+Del 可配合处理 UAC 框）。

**服务装不上？** 设置窗口必须以管理员身份运行（右键 → 以管理员身份运行）。

---

*版本 v1.0.0 · Rust 实现 · 被控端 1.7MB（Win7 版 2.1MB）/ 主控端 1.2~1.6MB，无任何运行时依赖*
