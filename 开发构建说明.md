# RControl 开发/构建说明

工作区: `rcontrol/`（rcontrol_common + rcontrol_server + rcontrol_client）

## 常规构建（Win10+ 被控端与主控端，MSVC 工具链）

```
cd rcontrol
cargo build --release
```

产物：`target/release/rcontrol_server.exe`（被控端）、`target/release/rcontrol_client.exe`（主控端）。

## Win7 兼容版（主控端 + 被控端，rustc 1.77.2 GNU 工具链）

Win7 上官方支持的最后一个 Rust 系列是 1.77，且需避免 UCRT/vcruntime140 依赖，
因此 Win7 版用 `x86_64-pc-windows-gnu` 工具链静态链接 CRT（依赖只剩系统 DLL + msvcrt.dll）。

```
rustup toolchain install 1.77.2 --profile minimal   # 本机默认 host 是 gnu
cd rcontrol
export CARGO_REGISTRIES_CRATES_IO_PROTOCOL=sparse
cargo +1.77.2 build --release -p rcontrol_client --target-dir target_win7   # 主控端
cargo +1.77.2 build --release -p rcontrol_server --target-dir target_win7   # 被控端
```

已知坑（都已处理）：

| 问题 | 处理 |
|---|---|
| 新版 cargo 把 Cargo.lock 写成 v4，cargo 1.77 解析失败 | `sed -i 's/^version = 4$/version = 3/' Cargo.lock` |
| cargo 1.77 默认 git 协议拉 index，与现有 sparse 缓存不匹配 | 环境变量 `CARGO_REGISTRIES_CRATES_IO_PROTOCOL=sparse` |
| indexmap 2.14 / hashbrown 0.17 等用了 edition 2024，1.77 解析不了 | `cargo update -p <crate> --precise <旧版>` 钉版本（见 Cargo.lock：indexmap 2.6.0、toml 0.8.14 + toml_edit 0.22.14 必须成对、embed-resource 2.4.3） |
| GNU 工具链 embed-resource 需要 windres，机器上没有 | build.rs 已做回退：MSVC 走 embed-resource；GNU 优先 windres，否则用 Windows SDK 的 rc.exe + cvtres.exe 手工编译 .rc → COFF .obj 链接（server 与 client 的 build.rs 同款） |
| 新版 tokio/mio/socket2/windows-service 把 windows-sys 拉到 0.61，改用 raw-dylib 链接，gnu 工具链需要 dlltool+as（rustup 自带的 dlltool 缺 as 子进程，跑不动） | 钉版本（Cargo.lock：tokio 1.46.1、mio 1.0.3、socket2 0.5.8、windows-service 0.8.0），windows-sys 落回 0.48/0.52/0.59 走预编译导入库；改 Cargo.toml 依赖版本时注意别把它们放掉 |
| 被控端 SendSAS 静态 `#[link(name="sas")]` 在 gnu 下 `-lsas` 找不到导入库 | input.rs 改为 LoadLibraryW + GetProcAddress 运行时解析 sas.dll（msvc 行为不变） |
| find_cvtres 搜 VS 布局时少一层（实际是 `<VS根>\<年份>\<产品>\VC\Tools\MSVC`，如 `2022\BuildTools`），曾导致 gnu 版资源一直静默跳过 | build.rs 已修；构建时若再见「资源编译跳过」警告要停下来查，不是无害的 |

钉版本后 stable 构建不受影响（低版本 semver 兼容）。

产物分发：`powershell -File shots/refresh_dist.ps1` 把四个 exe 刷进
`dist/被控端-Win2019以上`、`dist/被控端-Win7`、`dist/主控端-Win10以上`、`dist/主控端-Win7`。

## 自测

被控端跑起来后（任意模式）：

```
rcontrol_client.exe --selftest IP:端口 口令     # 11 项: 认证/画面/键鼠/文件/剪贴板, 退出码 0=全过
rcontrol_client.exe --view IP:端口 口令         # 跳过对话框直连
```

## GUI 截图验证（无头验证用）

```
powershell -File shots/shot.ps1 -Out out.png
```

## 服务模式手工操作（等价于 GUI 按钮）

```
sc create rcontrol binPath= "\"<绝对路径>\rcontrol_server.exe\" --service" start= auto
sc start rcontrol
sc stop rcontrol && sc delete rcontrol
netsh advfirewall firewall add rule name="RControl" dir=in action=allow protocol=TCP localport=3333
```

状态文件 `C:\ProgramData\rcontrol\status.json`（pid/session/system_priv），
日志 `C:\ProgramData\rcontrol\logs\`。

## 架构摘要（rustdesk 式）

- SCM 服务（SYSTEM, session 0）仅做看门狗：复制 winlogon.exe 令牌，用
  CreateProcessAsUserW 把 `--worker` 拉进活动控制台会话；会话切换/worker 被杀自动重拉。
- worker 负责全部工作：GDI 截屏（可切 winlogon 安全桌面，锁屏可见）、SendInput 注入、
  SendSAS（Ctrl+Shift+Del → 锁屏界面可输入密码登录）、文件、剪贴板。
- 协议：4 字节大端长度 + protobuf；明文握手（nonce 交换）→ HKDF 派生
  AES-256-GCM 会话密钥双向认证 → 全程加密。
