# 开发小结 2026-09-02：Win7 版被控端出包

## 背景与目标

原需求被控端目标系统为 Windows Server 2019，交付的 `被控端-Win2019以上`（msvc 工具链）
拷贝到 Windows 7 上无法执行。原因：Rust 1.78 起 msvc 目标只保证 Win10+，
Win7 兼容必须用 1.77.2 的 `x86_64-pc-windows-gnu` 工具链静态链接 CRT（此前仅主控端有 Win7 版）。

本次目标：补出 Win7 版被控端 `rcontrol_server.exe`，与 msvc 版功能完全一致。

## 交付物

| 文件 | 大小 | 说明 |
|---|---|---|
| `dist\被控端-Win7\rcontrol_server.exe` | 2.1MB | **新增**，Win7/Server 2008 R2 用 |
| `dist\被控端-Win2019以上\rcontrol_server.exe` | 1.75MB | 重编（SendSAS 实现改动同步） |
| `dist\主控端-Win7\rcontrol_client.exe` | 1.56MB | 重编（首次编入资源/manifest，修复按钮经典样式） |
| `dist\主控端-Win10以上\rcontrol_client.exe` | 1.2MB | 无源码改动，未动 |

`shots\refresh_dist.ps1` 升级为按 **被控/主控 × msvc/win7** 四路分发（含中文匹配，UTF-8 BOM 必需）。
使用说明 / 开发构建说明 两份文档已同步更新（工作区根 + dist 双份）。

## 代码与配置修改

1. **`rcontrol_server\build.rs`**：从 client 移植 GNU 资源编译回退（windres 缺失时用
   Windows SDK `rc.exe` + VS `cvtres.exe` 手工编 .rc → COFF .obj 交给 GNU ld）。
2. **`rcontrol_server\src\input.rs`**：`SendSAS` 由静态 `#[link(name="sas")]` 改为
   `LoadLibraryW("sas.dll") + GetProcAddress("SendSAS")` 运行时解析（注册表
   SoftwareSASGeneration 临时改写逻辑不变），两个工具链行为一致。
3. **`rcontrol_server\build.rs` + `rcontrol_client\build.rs`**：修 `find_cvtres` 两处 bug：
   - `read_dir(vs_root).ok()?` 在循环里把「第一个 VS 根不存在」放大成整函数早退（改 continue）；
   - VS 布局实际为 `<VS根>\<年份>\<产品>\VC\Tools\MSVC`（如 `2022\BuildTools`），
     旧代码把"2022"当产品层，cvtres 永远找不到。
4. **`Cargo.lock`**：钉版本 `tokio 1.46.1`、`mio 1.0.3`、`socket2 0.5.8`、`windows-service 0.8.0`
   （加上原有 indexmap 2.6.0 / toml 0.8.14+toml_edit 0.22.14 / embed-resource 2.4.3），
   windows-sys 落回 0.48/0.52/0.59。均为 semver 兼容降级，stable 构建不受影响。
5. **`shots\`** 新增回归脚本：`win7_srv_test.ps1`（win7 exe 起 3444 worker + selftest + 按 PID 清理）、
   `msvc_srv_test.ps1`（同款对 msvc exe）、`win7_gui_smoke.ps1`（GUI 冒烟）。

## 本次踩的三个坑（根因与结论）

### 坑 1：windows-sys 0.61 在 gnu 工具链上链接失败

现象：`error: Error calling dlltool 'dlltool.exe': program not found`，把 rustup 自带的
self-contained dlltool 加进 PATH 后变成 `dlltool.exe: CreateProcess`（它要 spawn `as.exe`，机器没有）。

根因：新版 tokio/mio/socket2/windows-service 把 windows-sys 解析到 0.61，该版本改用
**raw-dylib** 链接，rustc 需调用 dlltool（且依赖 as）生成导入库；旧版 windows-sys 走
windows-targets 预编译导入库，完全不需要这些。0828 的 win7 client 之所以能构建，
是因为 client 依赖图里没有 tokio（server 才有）。

处理：`cargo +1.77.2 update -p <crate> --precise <旧版>` 钉版本。注意 **tokio 对 mio 的
依赖是 `^1.0.1`，只降 tokio 的话 mio 仍会留在 1.2.2（要求 windows-sys ^0.61）**，必须显式
降 mio；socket2、windows-service 同理。

### 坑 2：`-lsas` 链接失败

现象：`ld: cannot find -lsas`。

根因：rustup 的 mingw 导入库集里没有 sas（SendSAS 是未文档化导出），SDK 只有 MSVC 格式的
`sas.lib`；静态 `#[link]` 无法规避。

处理：改运行时动态解析（见上），顺带免掉对导入库的存在性要求。

### 坑 3：gnu 版资源（manifest/版本信息/主题）自 0828 起一直静默跳过

现象：构建警告「资源编译跳过 (仅影响按钮主题样式): 找不到 cvtres.exe」，exe 照样产出。

根因：`find_cvtres` 的 VS 布局少一层目录（坑见上）。也就是说 0828 交付的两个 gnu exe
（主控端-Win7 等）其实都没带 manifest。**此警告不是无害的，构建时再见到必须停下来查。**

修复后特征：exe 增大约 1.6KB，警告消失。

## 验证结论

- **导入表**（dumpbin /DEPENDENTS + /IMPORTS）：仅依赖 advapi32/bcrypt/gdi32/kernel32/
  ntdll/oleaut32/user32/ws2_32/msvcrt，全部 Win7 原生；无 api-ms-*、无 UCRT/vcruntime140；
  扫描 Win8+ 独有 API 仅命中 `TryAcquireSRWLockExclusive`（MSDN 最低支持即 Win7）。
- **功能**：win7 exe 以 `--worker --user --port 3444` 启动，msvc 客户端 `--selftest`
  11/11 通过（退出码 0）；重编后的 msvc 被控端同样复跑通过。
- **GUI 冒烟**：win7 exe 设置窗口正常创建、CloseMainWindow 正常退出。
- **环境无污染**：测试全程走 3444，按 PID 精确清理；本机 G:\Tools\RControlV1 的
  3333 服务（GUI+看门狗+worker）未受影响。

## 遗留事项

- **真机 Win7 验证**：本机（Win11）只能保证导入表与运行时行为，最终需在真实 Win7 /
  Server 2008 R2 上装一次确认（管理员运行 → 安装服务 → 远程连接、锁屏 SAS、文件传输）。
- Win7 版构建对 Cargo.lock 的钉版本敏感：今后升级 tokio/mio/socket2/windows-service
  前先确认 gnu 构建仍通过（见 开发构建说明.md 坑表）。
